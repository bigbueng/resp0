# GitHub 连接器演示项目（Todo 小看板）

这是一个超轻量的前端演示项目，适合第一次连通仓库后用来展示：

- 基础页面结构（HTML）
- 页面样式（CSS）
- 可交互逻辑（JavaScript）

## 运行方式

在项目根目录执行：

```bash
python3 -m http.server 8000
```

然后打开浏览器访问：

- http://localhost:8000

## 可演示的交互

- 添加任务
- 勾选任务完成状态
- 删除单个任务
- 一键清除已完成任务
- 自动统计总数 / 完成数 / 完成率
